{
  "name": "@jobagent/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:format": "prisma format",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@jobagent/shared": "workspace:*",
    "@jobagent/core": "workspace:*",
    "@jobagent/embeddings": "workspace:*",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10"
  },
  "//": "Prisma scripts target schema.prisma (ADR-001/006); run db:generate/db:migrate once an embedding model is chosen (needs engine download). The pg-based repository persists discovery against the canonical schema applied by docs/database/schema.sql today."
}
